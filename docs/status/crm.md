# Status — CRM

tarefa: Item (a) concluído — os 3 cenários roteirizados desatualizados (`duvida_media_fecha_avista`, `divida_alta_aceita_agendamento`, `pacote_caro_recusa_vai_pro_selfservice`) foram corrigidos em `cenarios.ts` e re-verificados ao vivo contra produção, cada um percorrendo o grafo real sem repetição. De quebra, investiguei um achado novo do juiz no re-teste do `pacote_caro` ("Malala calculando preço em texto livre", R$9.930,00) — rastreei a cadeia de código completa e é 100% determinístico (`documentos_valores` → `valor_restricao_estimado` → `calcularFormulaAltoValor` → template puro), sem IA envolvida: falso positivo do juiz, não bug. Detalhes completos em `docs/superpowers/specs/2026-08-21-achados-bateria-testes-malala.md` (atualização 22/08/2026).
desde: 2026-08-22T00:00:00-03:00
proxima: Item (b) — investigar por que o classificador de desvio perde conteúdo embutido numa resposta já reconhecida (achado da rodada anterior: objeção/pergunta na MESMA mensagem que uma resposta válida some silenciosamente, porque o desvio só roda quando `naoReconhecido`). Depois: guia prático em Markdown pros outros módulos pedirem envio de comunicação ao CRM (seção 4 item 7 do quadro-branco). Na sequência: os 3 pedidos de melhoria no fluxo de Limpeza de Nome (inversão ln_passo17b/18, checkpoint inteligente de nomes pra limpar, novo passo de confirmação de contratação — design já discutido com Luiz, ainda não escrito em spec). Depois disso: nova bateria de testes completa.
bloqueio: nenhum bloqueio novo; ANTHROPIC_API_KEY na Vercel — falta confirmar redeploy; patch_abertura_email_justificativa_na_retomada.sql enviado, rodar não confirmado
turno_fim: 2026-08-22T00:15:00-03:00
