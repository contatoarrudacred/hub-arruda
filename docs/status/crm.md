# Status — CRM

tarefa: Concluído (SDD, worktree isolado) o plano de 14 tarefas da Comunicação Centralizada via CRM (`docs/superpowers/plans/2026-08-22-comunicacao-centralizada-crm.md`, spec em `docs/superpowers/specs/2026-08-22-comunicacao-centralizada-crm-design.md`). Migration já rodada pelo Luiz em produção. Mecanismo completo implementado e verificado: módulo `src/lib/comunicacao/` (lógica pura de resolução de instância + idempotência, TDD), suporte multi-instância no `zapster.ts` (instância oficial/secundária), categorias de comunicação administráveis (tela admin), aviso visível na Tela de Atendimento quando outro módulo manda mensagem pelo mecanismo, e Vendas religado pra usar `enviarComunicacao` em vez de chamar a API do WhatsApp/e-mail direto. Verificação final: `tsc --noEmit` e `eslint .` limpos; `vitest run` com 695 testes passando e 1 falha pré-existente conhecida (sem relação, em `engine.test.ts`, módulo motor-fluxo/agendamento).
desde: 2026-08-22T15:14:35-03:00
proxima: Os 3 pedidos de melhoria no fluxo de Limpeza de Nome: (1) inverter a ordem `ln_passo17b`/`ln_passo18`; (2) checkpoint inteligente pra decidir quais nomes de fato precisam de limpeza; (3) novo passo de confirmação de contratação. Depois disso: nova bateria de testes completa (achados pendentes em `docs/superpowers/specs/2026-08-21-achados-bateria-testes-malala.md`).
bloqueio: nenhum bloqueio pra esta tarefa (concluída). Pendência conhecida, fora do escopo do worktree: o Luiz ainda precisa configurar de verdade a instância secundária no painel Zapster — env vars `ZAPSTER_SECUNDARIO_API_TOKEN` / `ZAPSTER_SECUNDARIO_INSTANCE_ID` / `ZAPSTER_SECUNDARIO_WEBHOOK_SECRET` e a URL do webhook apontando pro número secundário no painel deles. Isso é o Coordenador quem avisa separadamente ao Luiz; registrado aqui só pra rastreio.
turno_fim: 2026-08-22T16:15:00-03:00
