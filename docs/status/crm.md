# Status — CRM

tarefa: Comunicação Centralizada via CRM — CONCLUÍDA e já em `main` (push feito, commit `d994993`). Mecanismo `src/lib/comunicacao/` completo: resolução de instância oficial/secundária + idempotência (TDD), categorias administráveis (tela em Configurações), aviso na Tela de Atendimento, Vendas religado via `enviarComunicacao`, guia prático em `docs/guias/comunicacao-centralizada.md`, regra dura registrada em `AGENTS.md` (nenhum módulo manda comunicação direto, todo envio passa por `enviarComunicacao`). Revisão final de branch inteira encontrou e corrigiu 2 bugs críticos (motor de fluxo colidindo conversa oficial/secundária; resposta manual do atendente saindo pelo número oficial) + 3 importantes (e-mail na fila de WhatsApp; retry duplicando link de pagamento; corrida na idempotência) antes do merge. Luiz decidiu: e-mail do mecanismo é aviso obrigatório, não respeita opt-out de marketing (nota de rodapé adicionada no template). Verificação final pós-merge: `tsc`/`eslint` limpos, 696/697 testes (1 falha pré-existente sem relação, `engine.test.ts`).
desde: 2026-08-22T18:35:00-03:00
proxima: Os 3 pedidos de melhoria no fluxo de Limpeza de Nome: (1) inverter a ordem `ln_passo17b`/`ln_passo18`; (2) checkpoint inteligente pra decidir quais nomes de fato precisam de limpeza; (3) novo passo de confirmação de contratação. Depois disso: nova bateria de testes completa (achados pendentes em `docs/superpowers/specs/2026-08-21-achados-bateria-testes-malala.md`).
bloqueio: nenhum. Pendência conhecida fora do escopo: Luiz vai configurar a instância secundária de verdade no painel Zapster (env vars `ZAPSTER_SECUNDARIO_API_TOKEN`/`ZAPSTER_SECUNDARIO_INSTANCE_ID`/`ZAPSTER_SECUNDARIO_WEBHOOK_SECRET` + webhook) e avisar quando pronto. Melhorias futuras não bloqueantes: categoria "Cobrança" referenciada por nome no Vendas (fragilidade aceitável), `contarNaoLidas` não filtra canal (miscount de badge, baixa severidade).
turno_fim: 2026-08-22T18:35:00-03:00
