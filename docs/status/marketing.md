# Status — Marketing

tarefa: Três correções nas imagens do pipeline, todas reportadas pelo Luiz e concluídas. (1) Capas saindo quase sempre com o mesmo ambiente (personagem sentado à mesa de casa) — achado real: bug de wiring em gerarCapa (capa.ts) fazia a etapa 3 (cruzamento, decide a "ideia visual única" incluindo o ambiente) computar um resultado que a etapa 4 (prompt final) nunca usava; e mesmo corrigido isso, nada impedia a IA de convergir sempre pro ambiente "mais seguro" (mesmo padrão de convergência já visto no tipo de ângulo do texto). Corrigido: etapa 4 recebe a ideiaVisual da etapa 3, e um catálogo de 14 ambientes cotidianos é sorteado (Math.random uniforme, sem migration — pedido explícito "de forma aleatória") e injetado como obrigatório nas duas etapas. (2) Posts saindo sem imagem de capa/secundárias intermitentemente — confirmado com dados reais: 6 de 14 execuções recentes de gerar_imagens puladas pelo guard de 220s porque gerar_conteudo sozinho já consome até 235s dos 290s da rota. Correção em 2 partes: (2a) capa e secundárias agora geram em PARALELO em vez de sequência (Promise.all em processar-pauta.ts) — reduz o tempo de gerar_imagens quando ele roda; (2b) correção estrutural pedida pelo Luiz — quando o orçamento fica curto, a pauta agora PAUSA (post fica persistido, pronto_para_publicar=false, pauta continua em_producao) em vez de publicar sem imagem pra sempre; nova função carregarPautaAguardandoImagens (repositorio.ts) é checada com prioridade MÁXIMA no topo de processarProximaPauta (antes de selecionarPauta) e retoma direto da geração de imagens na próxima invocação, com o relógio zerado de novo — sem precisar esperar o reclaim de 10min. 686/687 testes (só 1 falha pré-existente não relacionada, do CRM), tsc/eslint limpos. Tudo já commitado e no main.
desde: 2026-08-22T14:30:00-03:00
proxima: acompanhar produção nos próximos dias — confirmar que a frequência de posts sem imagem caiu e que a retomada funciona de verdade num cenário real de pausa
bloqueio: nenhum
turno_fim: 2026-08-22T16:00:00-03:00
