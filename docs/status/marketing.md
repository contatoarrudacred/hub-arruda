# Status — Marketing

tarefa: Duas correções em andamento nas imagens do pipeline, ambas reportadas pelo Luiz. (1) CONCLUÍDA: capas saindo quase sempre com o mesmo ambiente (personagem sentado à mesa de casa) — achado real: bug de wiring (`gerarCapa`, capa.ts) fazia a etapa 3 (cruzamento, decide a "ideia visual única" incluindo o ambiente) computar um resultado que a etapa 4 (prompt final) nunca usava, cada uma reinventando a cena isoladamente; e mesmo corrigido isso, nada impedia a IA de convergir sempre pro ambiente "mais seguro" (mesmo padrão de convergência já visto no tipo de ângulo do texto). Corrigido: etapa 4 agora recebe a `ideiaVisual` da etapa 3, e um `CATALOGO_AMBIENTES_CENA` (14 ambientes cotidianos) é sorteado (`Math.random`, uniforme, sem peso por recência — pedido explícito do Luiz "de forma aleatória", sem precisar de migration) e injetado como obrigatório nas duas etapas. (2) EM INVESTIGAÇÃO: posts saindo sem imagem de capa/secundárias intermitentemente — confirmado com dados reais de produção: 6 de 14 execuções recentes de `gerar_imagens` foram puladas pelo guard de orçamento de tempo (`LIMITE_MS_PARA_TENTAR_GERAR_IMAGENS = 220_000`, processar-pauta.ts) porque `gerar_conteudo` sozinho já consome até 235s (p90 216s) do teto de 290s da rota — não sobra tempo pra imagens no mesmo invocation. Ainda não implementada a correção (opções: paralelizar gerarCapa+gerarImagensSecundarias, e/ou separar geração de imagem numa invocação própria com orçamento de tempo fresco, como o Luiz sugeriu). 677/678 testes (só 1 falha pré-existente não relacionada, do CRM), tsc/eslint limpos na correção (1) já commitada.
desde: 2026-08-22T14:30:00-03:00
proxima: decidir e implementar a correção do timeout de gerar_imagens (item 2 acima)
bloqueio: nenhum
turno_fim: 2026-08-22T15:00:00-03:00
