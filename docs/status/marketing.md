# Status — Marketing

tarefa: Sorteio de tipo de ângulo (diversidade de conteúdo) — feature completa. O Luiz percebeu que os posts gerados saíam muito parecidos no ângulo retórico; causa raiz confirmada com dados reais: o catálogo de 15 tipos de ângulo (doc seção 5.1) nunca tinha sido implementado no código. Decisão do Luiz (não uma sugestão minha): sortear o TIPO por "menos usado recentemente" antes de escolher/gerar o ângulo em si, em vez de deixar alguma lógica "decidir" o tipo. Implementado: catálogo/tipos em tipos.ts, migration `20260822090000_marketing_tipo_angulo.sql` (escrita, aguardando o Luiz aplicar — registrada na seção 2 da coordenação), repositorio.ts (parse do novo shape `{texto,tipo}[]`, `carregarUltimoUsoPorTipoAngulo`, `criarPautaDePersona` grava o tipo), estrategista.ts reescrito (sorteia tipo → filtra personas candidatas → sorteia persona → sorteia ângulo dentro do tipo), gerador-angulo.ts recebe `tipoRequerido` no prompt (fallback de IA), UI da Agenda (chips de ângulo pronto mostram o tipo), e o script one-off `scripts/reclassificar-angulos-prontos.ts` pra reclassificar os ~347 ângulos já escritos pelo Luiz (ainda não rodado — depende da migration estar aplicada primeiro). 660/660 testes (só 1 falha pré-existente não relacionada, do CRM), tsc/eslint limpos.
desde: 2026-08-22T09:00:00-03:00
proxima: depois do Luiz aplicar a migration 20260822090000, rodar `pnpm exec tsx scripts/reclassificar-angulos-prontos.ts` contra o banco real e revisar o log de personas puladas
bloqueio: migration 20260822090000 escrita, aguardando o Luiz aplicar no SQL Editor antes do script de reclassificação poder rodar
turno_fim: 2026-08-22T13:00:00-03:00
